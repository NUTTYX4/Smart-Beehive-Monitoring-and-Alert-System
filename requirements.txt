# ============================================================
# Smart Beehive Monitor -- Raspberry Pi 4 (Bookworm 64-bit)
# pip install -r requirements.txt
# ============================================================

# Core
numpy<2
scipy>=1.11
certifi>=2024.2.2
requests>=2.31
psutil>=5.9

# Telegram bot (async v20+ API)
python-telegram-bot>=20.7,<21.0

# GPIO — RPi.GPIO works fine on Pi 4 Bookworm
RPi.GPIO>=0.7.1
gpiozero>=2.0

# I2C / weight sensor
smbus2>=0.4.3
hx711>=1.1.2.3

# DHT22 temperature / humidity (CircuitPython driver)
adafruit-circuitpython-dht>=4.0.0
adafruit-blinka>=8.20.0

# INMP441 I2S microphone
sounddevice>=0.4.6

# AI acoustic inference
librosa>=0.10
# tflite-runtime on Pi 4 (aarch64 or armv7l):
tflite-runtime>=2.14 ; platform_machine == "armv7l" or platform_machine == "aarch64"
# Full tensorflow only needed on x86 for training:
tensorflow>=2.15 ; platform_machine == "x86_64" or platform_machine == "AMD64"

# Computer vision — use system OpenCV installed via apt for Pi 4
# Run:  sudo apt install python3-opencv -y
# The pip package is a fallback for non-Pi environments:
opencv-python>=4.8 ; platform_machine != "aarch64" and platform_machine != "armv7l"

# YOLOv8 Nano inference
ultralytics>=8.1

onnx>=1.15.0
onnxruntime>=1.17.0
flask>=3.0.0
werkzeug>=3.0.0

