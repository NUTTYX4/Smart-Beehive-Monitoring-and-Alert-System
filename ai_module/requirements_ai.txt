# AI Module Dependencies
# =======================
# These are NOT part of the main requirements.txt to keep the Pi
# install lean.  Install selectively depending on your platform.

# ------ Training (PC only) ------
tensorflow>=2.15
librosa>=0.10
scipy>=1.11

# ------ Inference (Raspberry Pi) ------
# Install ONE of these on the Pi:
#   pip install tflite-runtime
# OR if you already have full TensorFlow installed, it works too.
# tflite-runtime>=2.14

# librosa is also needed on the Pi for MFCC extraction:
# librosa>=0.10
